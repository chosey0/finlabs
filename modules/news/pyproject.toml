[project]
name = "finlabs-news"
version = "0.1.0"
description = "RSS news collection, normalization, and analysis pipeline for FinLabs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.1.0",
    "feedparser>=6.0.12",
    "broker-modules",
    "httpx>=0.27.0",
    "rich>=13.0.0",
    "typer>=0.12.0",
]

[tool.uv.sources]
broker-modules = { git = "https://github.com/chosey0/broker-modules.git" }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
dev-mode-dirs = ["../.."]

[tool.hatch.build.targets.wheel]
only-include = [
    "__init__.py",
    "articles",
    "db",
    "entities.py",
    "main.py",
    "naver",
    "pipeline.py",
    "rss",
    "schema",
    "symbols.py",
]

[tool.hatch.build.targets.wheel.sources]
"" = "modules/news"
